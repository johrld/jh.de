services:
  jh-de:
    build:
      context: .
      dockerfile: dockerfile
      args:
        - SITE_URL=${SITE_URL_PROD:-https://www.jh.de}
    environment:
      - NODE_ENV=production
    ports:
      - "${PROD_PORT:-3000}:3000"
    restart: always
    networks:
      - dokploy-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

networks:
  dokploy-network:
    external: true
